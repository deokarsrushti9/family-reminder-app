
  # Family Reminder App UI

  This is a code bundle for Family Reminder App UI. The original project is available at https://www.figma.com/design/Mtc8WrIX4ymX3IfzFXd2Yf/Family-Reminder-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  